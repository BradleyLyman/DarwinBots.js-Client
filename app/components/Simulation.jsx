var React       = require('react');

var Simulation = React.createClass({
  render : function() {
    return (
      <div>
        <p>aoeu</p>
      </div>
    );
  }
});

module.exports = Simulation;
