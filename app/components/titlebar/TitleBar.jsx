var React = require('react');

module.exports = React.createClass({
  render : function() {
    return (
      <nav className="top-nav blue-grey">
        <div className="nav-wrapper">
            <a href="#" className="brand-logo">DarwinBots.js</a>
        </div>
      </nav>
    );
  }
});
